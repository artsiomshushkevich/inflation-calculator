Stage changes, create meaningful commit and push changes. Name commit based on content, use conventional commits format <type>: <description>.

Conventional commit types include feat (new features), fix (bug fixes), docs (documentation), style (code style), refactor (code restructuring), perf (performance improvements), test (tests), build (build system changes), ci (continuous integration), chore (maintenance), and revert (reverting a previous commit).
